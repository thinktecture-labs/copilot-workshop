pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{js,ts,vue}'
      run: npx eslint {staged_files}
    format:
      glob: '*.{js,ts,vue,css,md,json,yml,yaml}'
      run: npx prettier --check {staged_files}
